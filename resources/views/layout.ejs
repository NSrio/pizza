<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset ="UTF-8">
        <meta name="viewport" content="width=device-width,initial-scale=1.0">
        
        <title>Realtime pizza</title>
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Lato:wght@300;400;700&display=swap" rel="stylesheet">
        <link href="https://unpkg.com/tailwindcss@^2/dist/tailwind.min.css" rel="stylesheet">
        <link rel="stylesheet"
        href="https://maxst.icons8.com/vue-static/landings/line-awesome/line-awesome/1.3.0/css/line-awesome.min.css">
        <link rel="stylesheet" href="/css/app.css">
        
       
    </head>
    <body>
       
      
        <nav class="container mx-auto flex items-center justify-between py-4">
            <div>
               <a href="/"><img src="/img/logo.png" alt="logo"></a> 
            </div>
            <div>
            <ul class="flex item-center">
                <li class="ml-5"><a href ="/#menuid">Menu</a></li>
                
                <% if(!user) { %>
                    <li class="ml-5"><a href ="/register">Register</a></li>
                    <li class="ml-5"><a href ="/login">Login</a></li>
                   
                <% } else { %>

                    <li class="ml-5">
                        <form id="logout" action = "/logout" method="POST" > 
                            <a onclick = "document.getElementById('logout').submit()" href = "#">Logout</a>
                        </form>
                    </li>
                
                <% } %>
                <li class="ml-5"><a href ="/cart" class="inline-block px-4 py-0.5 rounded-full flex items-center">
                    <span id="cartcounter" class="text-white font-bold pr-1 pb-1.5"><%=session.cart ? session.cart.totalQty:''%></span>
                    <img class="pb-1"src="/img/cart.png" alt=""></a></li>
            </ul>
            </div>
        </nav>
        <%- body %>
    </body>
   <script src="/socket.io/socket.io.js"></script>
    <script src="/js/app.js"></script>
</html>